# ================================
# ChatFlect API - CORS & Delivery
# ================================

# ----------------
# AUTHORIZATION HEADER FIX (CRITICAL for 401 Unauthorized fix)
# ----------------
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTP:Authorization} ^(.*)
    RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]
</IfModule>

# ----------------
# UPLOADS DIRECTORY SAFETY
# ----------------
# Ensure uploaded files are served directly (no rewrite interference)
<IfModule mod_rewrite.c>
    # Note: RewriteEngine On is already declared above
    RewriteCond %{REQUEST_URI} ^/api/uploads/
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^(.*)$ - [L]
</IfModule>

# ----------------
# STATIC ASSETS CORS (Best Practice)
# ----------------
# Only apply Apache-level headers to static files (images, video).
# PHP files (.php) are handled by db.php to prevent "Duplicate Header" errors.
<FilesMatch "\.(jpg|jpeg|png|gif|webp|mp4|mp3|ogg|pdf)$">
    <IfModule mod_headers.c>
        Header set Access-Control-Allow-Origin "*"
        Header set Access-Control-Allow-Methods "GET, OPTIONS"
        Header set Access-Control-Allow-Headers "Range"
        Header set Accept-Ranges bytes
        Header set Cache-Control "public, max-age=31536000, immutable"
    </IfModule>
</FilesMatch>
