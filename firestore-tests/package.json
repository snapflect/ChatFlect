{
    "name": "firestore-tests",
    "version": "1.0.0",
    "description": "Firestore security rules test suite for ChatFlect",
    "main": "firestore.rules.test.js",
    "scripts": {
        "test": "mocha firestore.rules.test.js --timeout 10000"
    },
    "devDependencies": {
        "@firebase/rules-unit-testing": "^3.0.0",
        "firebase-admin": "^12.0.0",
        "mocha": "^10.2.0"
    }
}